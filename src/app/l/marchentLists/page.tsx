import Marchent from "@/components/marchent/Marchent";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Marchent />
    </div>
  );
};

export default page;
