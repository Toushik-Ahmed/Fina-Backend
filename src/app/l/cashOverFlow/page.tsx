import CashOverFlow from '@/components/cashOverflow/CashOverFlow';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <CashOverFlow />
    </div>
  );
};

export default page;
