import NetWorth from '@/components/netWorth/NetWorth';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <NetWorth />
    </div>
  );
};

export default page;
